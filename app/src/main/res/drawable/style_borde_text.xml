<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">

    <stroke
        android:width="1sp"
        android:color="@color/primary_text" />

    <!-- Padding para que el texto no se pegue con el borde -->
    <padding
        android:bottom="5sp"
        android:left="5sp"
        android:right="5sp"
        android:top="5sp" />

    <solid
        android:color="@color/fondo" />

</shape>